name: Deploy using connectors monkey

on:
  workflow_call:
    inputs:
      cloud-provider: 
        required: false
        type: string
        default: local
      metadata:
        required: false
        type: string
        default: "[ orphan ]"
      host: 
        required: false
        type: string
        default: '/'
    secrets:
      cloud-token:
        required: false
      github-token:
        required: false
      dockerhub_username:
        required: false
      dockerhub_token:
        required: false

jobs:      
  build-local:
    runs-on: ubuntu-22.04
    if: inputs.cloud-provider == 'local' 
        
    steps:
              
      - name: local condition
        run: | 
          echo ${{ inputs.cloud-provider }}     
                  
  build-civo:
    runs-on: ubuntu-22.04
    if: inputs.cloud-provider == 'civo'

    steps:      
      - name: civo conditions
        run: |
          echo ${{ inputs.cloud-provider }}

#     1. build repo with Dockerfile
      
      - uses: actions/checkout@v4

      
#      - name: Set up QEMU
#        uses: docker/setup-qemu-action@v3

#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3

#      - name: Login to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.dockerhub_username }}
#          password: ${{ secrets.dockerhub_token }}

#      - name: Build and push
#        uses: docker/build-push-action@v6
#        with:
#          push: true
#          tags: yabinm/app:${{github.sha}}

#      - name: docker ps
#        run: |
#          docker image ls
#          docker ps
#          ls

#     2. clone & exec helm
      - name: clone and exec helm
        id: exist-repo
        env:
          GH_TOKEN: ${{ secrets.github-token }}
        run: |
          echo "EXIST_REPO1=$(gh search repos helm --owner=snow-monkeys-org --visibility=private)" >> $GITHUB_OUTPUT
          echo $EXIST_REPO1
          echo "EXIST_REPO=$(gh search repos ${{ github.event.repository.name }}-helm-civo)" >> $GITHUB_OUTPUT
          echo $EXIST_REPO

      
#     if repo already exist
      - name: repo already exist for helm
        if:  ${{ steps.exist-repo.outputs.EXIST_REPO1 }} != no repositories matched your search
        env:
          GH_TOKEN: ${{ secrets.github-token }}
        run: |
          echo repo1 already exist ${{ steps.exist-repo.outputs.EXIST_REPO1 }}
          echo repo2 already exist ${{ steps.exist-repo.outputs.EXIST_REPO }}

#     if repo doesnt exist
#      - name: repo doesnt exist for helm
#     env:
#     GH_TOKEN: ${{ secrets.github-token }}
#     run: |
#  echo repo already exist ${{ env.EXIST_REPO }}
        
